"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CdaSearchApi = void 0;
const axios_1 = __importDefault(require("axios"));
const cheerio_1 = __importDefault(require("cheerio"));
const errors_1 = require("./errors");
axios_1.default.defaults.withCredentials = true;
class CdaSearchApi {
    static search(query, page = 1) {
        return __awaiter(this, void 0, void 0, function* () {
            let res = yield axios_1.default.get(encodeURI(`https://www.cda.pl/video/show/${query.replace(/[\/\\ ]/gm, "_")}/p${page}`));
            let resultsdoc = res.data;
            let $ = cheerio_1.default.load(resultsdoc);
            if ($('h2[style="float: none; display: block;"]').text() === "Strona, którą próbujesz oglądać nie istnieje")
                throw new errors_1.NotFoundError(`Page ${page} not found`);
            let results = $("div.video-clip-wrapper");
            let ret = [];
            results.toArray().forEach((elem) => {
                let ele = cheerio_1.default.load($.html(elem));
                if (ele("a.folder-item")[0])
                    return;
                let time = ele("span.timeElem").text();
                let miniatureURL = "https:" + ele("img.video-clip-image").prop("src");
                let title = ele("a.link-title-visit").text();
                let link = "https://www.cda.pl" + ele("a.link-title-visit").attr("href");
                ret.push({
                    title,
                    miniatureURL,
                    duration: time,
                    link
                });
            });
            return ret;
        });
    }
}
exports.CdaSearchApi = CdaSearchApi;
