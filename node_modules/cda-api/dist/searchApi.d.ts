export interface SearchResult {
    title: string;
    miniatureURL: string;
    duration: string;
    link: string;
}
export declare class CdaSearchApi {
    static search(query: string, page?: number): Promise<SearchResult[]>;
}
